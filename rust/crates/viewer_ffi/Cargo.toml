[package]
name = "viewer_ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
base64 = "0.22.1"
format_docx = { path = "../format_docx" }
serde.workspace = true
serde_json = "1.0.145"
viewer_core = { path = "../viewer_core" }

[dev-dependencies]
tempfile = "3.15.0"
zip = { version = "2.4.2", default-features = false, features = ["deflate"] }
