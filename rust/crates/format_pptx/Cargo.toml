[package]
name = "format_pptx"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
base64 = "0.22.1"
format_shared = { path = "../format_shared" }
viewer_core = { path = "../viewer_core" }

[dev-dependencies]
tempfile = "3.18.0"
zip = { version = "2.4.2", default-features = false, features = ["deflate"] }
